/**
 * 현재 레벨에서 다음 레벨까지 필요한 경험치.
 * 정본: `shared/utils/strategyLevelXp.ts` — 클라이언트·서버 공통.
 */
export { getXpRequirementForLevel as getXpRequiredForCurrentLevel } from '../shared/utils/strategyLevelXp.js';
