# Railway Dashboard 현재 설정 확인 및 수정 가이드

## 현재 화면에서 확인된 설정

### ✅ 이미 잘 설정된 항목

1. **Resource Limits**
   - CPU: **4 vCPU** ✅ (적절)
   - Memory: **8 GB** ✅ (적절)
   - Plan limit: 32 vCPU / 32 GB

2. **Healthcheck Path**
   - `/api/health` ✅
   - railway.json에 설정됨

3. **Healthcheck Timeout**
   - `60` ✅
   - railway.json에 설정됨

4. **Teardown**
   - Enable Teardown: **OFF** ✅ (비활성화)

5. **Serverless**
   - OFF ✅ (비활성화)

### ⚠️ 확인/수정이 필요한 항목

1. **Cron Schedule** ⚠️
   - 현재 설정: `0 1 * * *` (매일 1시에 실행)
   - **문제**: 웹 서버는 cron으로 실행할 필요 없음
   - **조치**: 비우기 또는 제거

## 수정해야 할 설정

### 1. Cron Schedule 제거 또는 비우기

**이유:**
- 웹 서버는 24/7 실행되어야 함
- Cron은 특정 시간에만 실행되는 스케줄 작업용
- Cron이 설정되어 있으면 서버가 스케줄 시간에만 실행될 수 있음

**조치 방법:**
1. **Cron Schedule** 섹션 찾기
2. 입력 필드에 있는 `0 1 * * *` 값 **삭제** (비우기)
3. 저장

### 2. Pre-deploy Command 확인

**확인 방법:**
- Settings → Deploy 섹션 아래로 스크롤
- **"Pre-deploy Command"** 섹션 찾기

**권장 설정:**
- 비우기 또는
- 최소화: `npx prisma db push --schema prisma/schema.prisma --accept-data-loss --skip-generate`

### 3. Custom Start Command 확인

**확인 방법:**
- Settings → Deploy 섹션
- **"Custom Start Command"** 섹션 찾기

**권장 설정:**
- 비우기 (railway.json의 startCommand 사용)
- 또는 `npm run start-server`

## 즉시 수정할 항목

### 🔴 최우선: Cron Schedule 제거

**현재 문제:**
- Cron Schedule: `0 1 * * *` 설정됨
- 이것이 서버가 24/7 실행되지 않는 원인일 수 있음

**조치:**
1. **Cron Schedule** 입력 필드 찾기
2. `0 1 * * *` 값 **삭제** (비우기)
3. 저장

## 현재 설정 요약

### ✅ 유지할 설정
- Resource Limits: 4 vCPU / 8 GB ✅
- Healthcheck Path: `/api/health` ✅
- Healthcheck Timeout: `60` ✅
- Restart Policy: `Never` ✅
- Teardown: OFF ✅
- Serverless: OFF ✅

### ⚠️ 수정할 설정
- **Cron Schedule**: 비우기 ⚠️
- **Pre-deploy Command**: 확인 및 최소화

## 수정 순서

1. **Cron Schedule 제거** (가장 중요)
   - `0 1 * * *` 삭제
   - 입력 필드 비우기

2. **Pre-deploy Command 확인**
   - 내용 확인
   - 필요시 비우기 또는 최소화

3. **저장 후 재배포**
   - "Apply 3 changes" 버튼 클릭
   - 또는 "Deploy ⇧+Enter" 클릭

## 결론

**가장 중요한 수정:**
- **Cron Schedule을 비우세요!** 
- 이것이 서버가 정상적으로 24/7 실행되지 않는 주요 원인일 수 있습니다.

**다른 설정은 모두 적절합니다:**
- Resource Limits: 4 vCPU / 8 GB ✅
- Health Check: 잘 설정됨 ✅
- Restart Policy: Never ✅

