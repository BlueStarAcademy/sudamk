# 공통 환경 변수 예시
NODE_ENV=production

# Database connection (Prisma / Supabase)
# Replace with your Supabase-provided connection string.
# Example: postgresql://postgres:password@db.<hash>.supabase.co:5432/postgres
DATABASE_URL=

# KataGo 설정
# 배포 환경에서 KataGo HTTP API를 사용하려면 아래 URL을 설정하세요.
# 예: https://katago-api.yourdomain.com/api/analyze
# 또는: http://localhost:8080/api/analyze (로컬 테스트용)
# 이 값이 설정되어 있으면 로컬 KataGo 프로세스 대신 HTTP API를 사용합니다.
# 로컬 개발 환경에서는 이 값을 설정하지 않으면 KataGo가 비활성화됩니다.
KATAGO_API_URL=

# KataGo 로컬 프로세스 설정 (KATAGO_API_URL이 없을 때만 사용)
# docker-compose에서 override 가능
KATAGO_NUM_ANALYSIS_THREADS=4
KATAGO_NUM_SEARCH_THREADS=8
KATAGO_MAX_VISITS=500
KATAGO_NN_MAX_BATCH_SIZE=8

# 이메일 서비스 설정 (AWS SES)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
EMAIL_FROM=noreply@yourdomain.com

# SMTP 설정 (개발 환경용, AWS SES 대신 사용 가능)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# 카카오 로그인 설정
KAKAO_CLIENT_ID=
KAKAO_CLIENT_SECRET=
KAKAO_REDIRECT_URI=https://yourdomain.com/auth/kakao/callback

# 프론트엔드 URL (이메일 링크용)
FRONTEND_URL=https://yourdomain.com


