/**
 * UI 전용으로 `false`로 두면 데모 5인만 사용합니다.
 * 실제 매칭은 `true`(기본).
 *
 * 인게임 패널은 배경 알파를 대략 /90~/96(불투명도 ~80% 이상)로 두고, 블러는 최소화하는 편이 정보 가독성에 유리합니다.
 */
export const CHAMPIONSHIP_VERSUS_VENUE_USE_LIVE_OPPONENT_LIST = true;
